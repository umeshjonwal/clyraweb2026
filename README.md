# Cylra Ultra v2 — Agency Website (Competition Build)

A unique, competition-ready React + Tailwind site with next‑level animations, full mobile responsiveness (including tables), blog, and working navigation.

## Run locally
```bash
npm install
npm run dev
```
## Build
```bash
npm run build
npm run preview
```
